// import {extend} from '../../../utils';

const initialState = {
};

const logic = (state = initialState, action) => {
  switch (action.type) {

  default:
    return state;
  }
};

export default logic;
